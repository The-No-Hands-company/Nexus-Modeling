export type SystemsApiRegistrationPayload = { id: string; name: string; description: string; mode: "orchestrated" | "standalone"; exposed: boolean; health: "healthy" | "degraded" | "offline"; upstreamUrl: string; capabilities: string[]; metadata: Record<string, unknown>; };
export function buildSystemsApiRegistrationPayload(baseUrl: string): SystemsApiRegistrationPayload { return { id: "nexus-tutor", name: "Nexus-Tutor", description: "Tutoring lessons, quizzes, and student management service", mode: "orchestrated", exposed: false, health: "healthy", upstreamUrl: baseUrl, capabilities: ["lessons","quizzes","students","tutoring"], metadata: { version: "v1", defaultPort: 3111 } }; }
