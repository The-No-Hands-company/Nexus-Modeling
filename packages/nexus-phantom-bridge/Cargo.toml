[package]
name = "nexus-phantom-bridge"
version = "0.1.0"
edition = "2021"

[dependencies]
phantom-crypto = { path = "../../apps/Phantom/crates/phantom-crypto" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
hex = "0.4"
blake3 = "1"
rand = "0.8"
